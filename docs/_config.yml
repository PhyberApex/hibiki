# Jekyll config for GitHub Pages (Settings → Pages → Deploy from branch → main → /docs)
title: Hibiki
description: Discord audio companion bot — music, effects, and a Vue dashboard
# When served at https://<user>.github.io/hibiki/ the base URL is /hibiki
baseurl: "/hibiki"
url: "https://phyberapex.github.io"

markdown: kramdown
kramdown:
  input: GFM
  hardwrap: false
